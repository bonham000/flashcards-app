import React from 'react';

const About = () =>
  <div>
    <h1>Home Page</h1>
    <h2>This is a simple single page application built using React, React-Router and Redux.</h2>
    <h2>State persists in a single redux store as you navigate through the links above</h2>
  </div>;

export default About;
